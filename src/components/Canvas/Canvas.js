import React from 'react';
import PropTypes from 'prop-types';

import "./Canvas.scss";

const Canvas = ({}) => {
  return (
    <canvas id="canvas"></canvas>
  );
};

Canvas.propTypes = {

};

export default Canvas;